name: Ping Render API

on:
  schedule:
    - cron: "*/3 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl  https://netpredic.onrender.com/predict
